# VizDOM - Core dependencies
# Install: pip install -r requirements.txt

# Core
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
robotframework>=6.0
requests>=2.28.0

# OCR - EasyOCR (default backend)
easyocr>=1.7.0

# OCR - Tesseract (lightweight alternative)
pytesseract>=0.3.10

# LLM refinement (Ollama backend uses requests, no extra deps)
# For local model inference, install: pip install -r requirements-llm.txt
